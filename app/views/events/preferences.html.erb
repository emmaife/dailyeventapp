<!-- , url: user_neighborhoods_path -->
<%#= form_for(@neighborhoods) do |f| %> 
<%# @neighborhoods.each do |i| %>

  <%#= f.label :name %>
  <%#= f.check_box :id %>

  <%#= f.submit 'Save' %>
<%# end %>

<%= form_for(@user) do |f| %>
  <%= f.label :email %>
  <%= f.text_field :email %>
  <%= f.collection_check_boxes(:neighborhood_ids, @neighborhoods, :id, :name) %>
  <%= f.collection_check_boxes(:category_ids, @categories, :id, :name) %>

  <%= f.submit %>
<% end %>